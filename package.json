{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-lightbox-images.mjs && node scripts/pdf-to-image.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "lhci": "CHROME_PATH=/usr/bin/google-chrome lhci collect && CHROME_PATH=/usr/bin/google-chrome lhci assert",
    "lhci:results": "node scripts/print-results.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "pdf-to-img": "^6.1.0"
  }
}
